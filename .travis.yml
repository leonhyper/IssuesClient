dist: trusty
language: node_js
node_js:
- "stable"
cache:
  directories:
  - node_modules
before_script:
- npm install -g httpserver
- npm run build
script:
- npx cypress run
deploy:
  provider: surge
  skip_cleanup: true
  domain: your-domain.surge.sh
  project: ./dist
  on:
    branch: master

